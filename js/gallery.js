// Initialize Flickity
$(".main-carousel").flickity({
  // Flickity options
  wrapAround: true,
  adaptiveHeight: true,
  imagesLoaded: true,
});
// Initialize Flickity
$(".main-carousel").flickity({
  // Flickity options
  wrapAround: true,
  adaptiveHeight: true,
  imagesLoaded: true,
});
$('.main-carousel').flickity({
  // options
  wrapAround: true,
  adaptiveHeight: true,
  imagesLoaded: true
});